import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 6,
    },
    title: {
        color: "white",
        fontWeight: 'bold',
        fontSize: 28,
        margin: 8,
    },

})

export default styles